using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class Sphere : MonoBehaviour {
 //   public SerialReader serialReader;

	//void Update () {
 //       if (serialReader && serialReader.isConnected) {
 //           float scale = serialReader.data.potValue * 5.0f;
 //           gameObject.transform.localScale = new Vector3(scale, scale, scale);

 //           Renderer sphereRenderer = gameObject.GetComponent<Renderer>();
 //           if (sphereRenderer && sphereRenderer.material) {
 //               sphereRenderer.material.color = serialReader.data.isButtonDown
 //                   ? Color.white
 //                   : Color.black;
 //           }
 //       }
	//}
}
